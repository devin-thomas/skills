interface:
  display_name: "Execute Task Cycles"
  short_description: "Run execute-task in bounded sequential cycles"
  default_prompt: "Use $execute-task-cycles with max_cycles=3 to execute successive repository tasks until the limit or a stopping condition is reached."
