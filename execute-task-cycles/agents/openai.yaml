interface:
  display_name: "Execute Task Cycles"
  short_description: "Run an explicit multi-task execution loop"
  default_prompt: "Use $execute-task-cycles to work through the remaining repository tasks until the project is complete or a stopping condition is reached."
